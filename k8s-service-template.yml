apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: cesieats-services
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: delivery-service
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      containers:
        - name: delivery-service
          image: kilme/delivery-service:$IMAGE_VERSION
          resources:
            limits:
              memory: "512Mi"
              cpu: "250m"
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: service-delivery
  namespace: cesieats-services
spec:
  selector:
    app: delivery-service
  ports:
    - name: http
      protocol: TCP
      port: 3000
      nodePort: 30199
  type: NodePort